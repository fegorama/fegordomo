import { DeviceItemDirective } from './device-item.directive';

describe('DeviceItemDirective', () => {
  it('should create an instance', () => {
    const directive = new DeviceItemDirective();
    expect(directive).toBeTruthy();
  });
});
